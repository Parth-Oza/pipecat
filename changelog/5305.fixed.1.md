- Fixed `CartesiaTTSService` hardcoding `pause_frame_processing=False` ahead of `**kwargs`, which made the value impossible to override — passing `pause_frame_processing=True` raised `TypeError: got multiple values for keyword argument`. It is now a default, so applications can enable frame-processing pause and with it the `pause_watchdog_timeout_s` guard against a stream that reports success but produces no audio.
